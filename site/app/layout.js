import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        <header style={{ background: '#27ae60', padding: '1rem 0' }}>
          <nav style={{
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            maxWidth: '900px',
            margin: '0 auto',
            padding: '0 1rem',
          }}>
            <span style={{
              fontFamily: 'Montserrat, Lato, Arial, sans-serif',
              fontWeight: 'bold',
              color: '#fff',
              fontSize: '2rem',
              letterSpacing: '0.05em',
            }}>
              Willie's Lavanderia
            </span>
            <div style={{ display: 'flex', gap: '1.5rem' }}>
              <a href="/" style={{ color: '#fff', fontWeight: 500 }}>Home</a>
              <a href="/about" style={{ color: '#fff', fontWeight: 500 }}>About</a>
              <a href="/services" style={{ color: '#fff', fontWeight: 500 }}>Services</a>
              <a href="/contact" style={{ color: '#fff', fontWeight: 500 }}>Contact</a>
            </div>
          </nav>
        </header>
        <main style={{ minHeight: '80vh', background: 'var(--background)' }}>{children}</main>
        <footer style={{
          background: '#27ae60',
          color: '#fff',
          textAlign: 'center',
          padding: '1rem 0',
          fontSize: '1rem',
        }}>
          &copy; {new Date().getFullYear()} Willie's Lavanderia. All rights reserved.
        </footer>
      </body>
    </html>
  );
}
